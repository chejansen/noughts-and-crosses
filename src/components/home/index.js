import React from 'react';
import Header from '../header';
import Grid from '../grid';

const Home = () => (
	<div className='page'>
		<Header title='Hi Lounge Buddy' />
		<Grid/>
	</div>
);

export default Home;
